import React from 'react'
import './styles/styles.css'

export const Stuff = (): React.ReactElement => {
    return <div className={'stuff-container'}>
        Stuff
    </div>
}

export default Stuff
